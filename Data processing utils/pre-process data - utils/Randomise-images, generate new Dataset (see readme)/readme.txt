This scriplet will scan through a regression dataset(training or validation), process each image,
apply the 3D or 9D randomisation algorithm, then save the output for different seed values.
It will also generate a new annotations.csv file that contains all seed value I/O.